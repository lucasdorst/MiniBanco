package com.example.MiniBanco.model.enums;

public enum TipoConta {
    //enum é uma lista fixa de valores, assim o usuario apenas pode escolher entre elas e nao precisa escrever nada

    CORRENTE,
    POUPANCA
}
